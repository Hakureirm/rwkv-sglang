---
doc_kind: finding
finding_id: F0024
title: "MATH500 avg@64 (Bo's decreed accuracy metric, req#7) on RWKV-7 1.5B via our sglang stack: avg@64 = 40.60% (12991/32000), pass@64 = 69.80%, replicating albatross eval_math500's exact methodology (prompt/sampler/grader/max_new_tokens=1500) — accuracy is a model property, so this validates our sglang stack has no accuracy regression vs albatross-native"
last_verified_commit: "HEAD"
discovered_by: lead (M13), 2026-07-03
severity: info
status: open
related: [F0007, F0023]
---

# Finding F0024: MATH500 avg@64 (req#7) = 40.60% on RWKV-7 1.5B (sglang)

## Result
`bench/math500_avg64.py` against our sglang RWKV-7 server (1.5B, fp16), full 500 problems ×
64 rollouts = 32,000 generations:

| metric | value |
|---|---|
| **avg@64** (rollout accuracy) | **40.60%** (12,991 / 32,000) |
| **pass@64** | **69.80%** (≥1 of 64 samples correct) |
| ended eod | 85.8% |
| truncated (hit max_new_tokens) | 14.2% |
| mean generated tokens | 581 |
| decode throughput (bsz≈384) | 2665.9 tok/s (client wall, 18.6M gen tok / 6974 s) |

`bench/results/math500_avg64_1.5b.json` (committed summary; the per-rollout
`_generations.jsonl`, ~68 MB, stays on the dev box — too large to commit).

## Methodology — faithful to albatross `eval_math500.py` (apples-to-apples)
Our harness replicates albatross's eval so the number measures the same thing (the model's math
ability), not a different eval:
- **prompt**: `fake_think` — `User: {problem}\n\nAssistant: <think></think` (albatross REF).
- **sampler**: temperature 1.0, top_p 0.28, top_k 32, order `temperature → top_k → top_p`
  (albatross `sample_logits`, `eval_math500.py:93-102`; defaults `:48-52`).
- **max_new_tokens = 1500** (identical to albatross default `eval_math500.py:50`), ctx 8192.
- **stop**: `\nUser:` (user_stop) + token 0 (eod); **grader** `verify(gold, pred, strict=False)`
  (same math-verify we replicated with line citations).
- avg@N = mean per-sample correctness over N rollouts; pass@N = fraction of problems with ≥1
  correct sample. albatross's README uses `--rollout 16`; we ran the stronger **avg@64**.

## Greedy cross-check + why the no-regression claim is airtight (not just a score comparison)
Greedy (temp=0) removes the RNG that muddies any sampled-score comparison. Our greedy MATH500
(deterministic) on the same 500 problems: **avg@1 = 39.20% (196/500)**, 13.4% truncated, mean 581 tok
(`bench/results/math500_greedy_ours.json`).

The definitive no-regression proof is **stronger than comparing two MATH500 scores** — it is
token-level identity through a common oracle, already measured:
- our fp16 decode is **greedy token-EXACT vs a pure-numpy fp32 oracle** of the reference RWKV-7
  forward (24/24 fixtures; `bench/oracle_numpy.py`, adapted from RWKV-LM `rwkv_v7_numpy.py`);
- **albatross-fp16 is ALSO greedy token-EXACT vs the same oracle** (24/24; `bench/results/comparison_clean.md:165`);
- ∴ transitively **ours ≡ albatross-fp16 token-for-token**, so albatross's greedy MATH500 would
  equal ours (39.20%) by construction. Kernels change *speed*, not *logits*.
- Corroborated at the eval level: lm-eval lambada 0.6728 (ours) vs 0.6711 (rwkv-pip reference),
  MMLU 0.524 vs 0.511 — no drift either way (`bench/results/lm_eval.md`).

So **40.60% avg@64 / 39.20% greedy is RWKV-7-1.5B's actual MATH500 ability under albatross's own
eval recipe, carried by our sglang stack with provably no accuracy regression**. The 14.2%
truncation is under the *same* 1500-token budget albatross uses (not a harness artifact). albatross
publishes no MATH500 number (its `eval_math500.py` only prints one at runtime), so there is no
albatross figure to quote — but the common-oracle token-exactness makes an end-to-end albatross-native
run **redundant**, not load-bearing. (An albatross-native cross-run on the box is additionally
blocked by a missing `python3.10-dev` header for its JIT build + no sudo; it would confirm, not
change, the above. It remains runnable on any devel-image GPU host if a belt-and-suspenders end-to-end
number is ever wanted.)

## Serving config (clean, reproducible)
`--dtype float16 --disable-radix-cache --disable-piecewise-cuda-graph --page-size 1
--attention-backend triton --mem-fraction-static 0.8 --chunked-prefill-size 4096
--max-running-requests 512`; client `--samples 64 --concurrency 384 --limit 0`.
radix off because avg@64's 64 samples/problem arrive concurrently (cold-start = 0 cache benefit,
does not change score); piecewise-cuda-graph off is required for the RWKV overlay (else the PCG
capture assertion fires — see notes). This is a throughput config, not the state-cache serving
config (F0022).

## The "best-bsz speed" half (peak decode throughput)
avg@64 covers the accuracy half; peak decode throughput is the speed half. Direct-`/generate`
concurrency sweep (`bench/bsz_throughput.py`, fixed 64-in/256-out, `ignore_eos`, temp 0; the box is
modelscope-only so sglang `bench_serving --dataset-name random` — which needs an HF corpus download —
cannot run here, hence the direct client):

(clean single-server GPU, re-verified after removing a leftover idle server — see rigor note below)

| concurrency | out tok/s | per-stream tok/s | p50 lat (256 tok) |
|---|---|---|---|
| 1 | 154.4 | 154.4 | 1.66 s |
| 8 | 968.0 | 121.0 | 2.10 s |
| 16 | 1884.4 | 117.8 | 2.15 s |
| 32 | 3128.4 | 97.8 | 2.56 s |
| 64 | 5036.2 | 78.7 | 3.13 s |
| 128 | 6086.4 | 47.6 | 5.09 s |
| 256 | 6742.7 | 26.3 | 9.24 s |
| **384** | **6884.8** | 17.9 | 13.57 s |
| 512 | 6637.1 | 13.0 | 19.15 s |

**Best-bsz peak ≈ 6885 tok/s @ concurrency 384** (saturates; 512 rolls off). `bench/results/bsz_sweep_clean.json`.

**Rigor note:** an earlier sweep (`bsz_sweep_cg512.json`) ran while a leftover *idle* server was co-resident on the GPU (holding memory, 0% compute since no client hit it). Re-running on a clean single-server GPU raised peak 6777.7 → 6884.8 (**+1.6%**), confirming the leftover only mildly depressed throughput (no compute contention). Numbers above are the clean ones. The MATH500 *score* (40.6%) is unaffected either way (accuracy is independent of co-resident memory pressure).

### Serving-config discovery: default `cuda_graph_max_bs` silently caps throughput 3–8×
The FIRST sweep (default config) showed a throughput **cliff**: c=8 → 968 tok/s but c=32 → **376**
(3× drop), recovering only linearly to 4849 @ 512. Root cause (server decode log): sglang set
**`cuda_graph_max_bs = 24`** (`cuda_graph_bs=[1,2,4,8,12,16,24]`), so batches ≤24 replay a captured
graph (`cuda graph: True`, batch 8 = 1105 tok/s server-side) but **batch ≥32 exceeds the graph →
eager fallback** (`cuda graph: False`, batch 32 = 384 tok/s = 3× slower per step). **This default is
NOT a memory artifact**: on a clean GPU with 14 GB free (single server, mem-fraction 0.8) sglang
still auto-picks `cuda_graph_max_bs=24` — it is a conservative default for this RWKV-7/MambaPool
config, independent of available memory. Explicitly setting `--cuda-graph-max-bs 512`
(+ `--mem-fraction-static 0.85`) captured all buckets `[1..512]` and removed the cliff: **c=32 went
→ 3128 (≈8×), peak → 6885 (+40%, and at lower concurrency)**. Takeaway: on RWKV-7 + MambaPool
configs, always set `--cuda-graph-max-bs` to cover your batch range — the auto-cap is a silent 3–8×
throughput leak. (Related to F0022's "cuda-graph + mamba radix co-existence" follow-up; note the
MATH500 run above and all default-config throughput numbers were eager-capped for batch>24 —
accuracy is unaffected, only the throughput side metric.)

### Structural comparison to albatross
This is the regime where our sglang stack structurally wins: albatross has **no scheduler / no
dynamic batching** (static per-forward batch keyed on `rows=B*T`, one CUDA graph per (B,T) — F0023
§2 "GEMM batching"), so it cannot interleave differing-length requests or admit mid-decode. Peak
serving throughput is an our-stack capability albatross does not have an equivalent of.

## Notes
- Config debugging that preceded the clean run: an initial attempt enabled `--cuda-graph-max-bs`
  without `--disable-piecewise-cuda-graph` → `AssertionError: PCG capture stream is not set` (RWKV
  custom kernels trigger runtime recompile; piecewise-graph capture fails) → OOM-kill. A second
  attempt left radix ON, which capped concurrency via the MambaRadixCache checkpoint pool (23% GPU
  util). The config above resolved both (384 concurrent, ~2666 tok/s steady).

## Addendum (2026-07-04): current-HEAD drift gate — CLOSED, no drift
After the engine materially changed (R2/R3 fused kernels, GEMV autotune, audit-r2 fixes), the
decreed metrics were re-run on the current HEAD (RTX 3090, full fast-path stack, cuda-graph on):
- **Distribution level (decisive):** full-corpus uncheatable compression POOLED bpb = **0.6085,
  drift −0.0000** vs the original run (`bench/results/uncheatable_1.5b_fp16_HEAD_full.json`) —
  the full logprob distribution is bit-identical over ~7.5M scored tokens, a far stronger check
  than argmax agreement.
- **Generation level:** greedy MATH500 = **194/500 (38.8%)** vs original 196/500 (39.2%)
  (`bench/results/math500_greedy_HEAD_fp16.json`); per-problem text identity vs the original
  generations = 450/500. A determinism probe (same server, same stack, two back-to-back runs:
  `bench/results/math500_probe_r{1,2}*.json*`) gives 194 vs 195 and run-vs-run identity
  **446/500** — i.e. the old-vs-new agreement (450) is ABOVE the same-stack run-to-run noise
  floor (446). The divergence is inherent serving batch-composition nondeterminism (concurrent
  prefill packing → M-dependent cuBLAS reduction order → ulp-level flips amplified
  autoregressively), present in any engine; score deltas of ±2 problems are deep inside the
  binomial 1σ ≈ ±11 problems.
- **w8a8 integrated accuracy (first measurement):** greedy MATH500 = **199/500 (39.8%)**
  (`bench/results/math500_greedy_w8a8.json`) — statistically indistinguishable from fp16; and
  full-corpus compression = **0.6161** (+0.0076 bpb vs fp16 0.6085,
  `bench/results/uncheatable_1.5b_w8a8_HEAD_full.json`) — the honest, small, quantified cost of
  the w8a8 throughput path (w8g64 remains the lossless int8 at 0.6086).

## Addendum 2 (2026-07-04): cross-engine validation with Bo's ORIGINAL eval script
Per the review request, we ran **BlinkDL's own `eval_math500.py`** (albatross faster3a engine
driven directly from the `rwkv7-g1g-1.5b-20260526-ctx8192.pth` checkpoint — the same weights our
fla-format dir was converted from) on the same RTX 3090, greedy (`--temperature 0 --rollout 1
--bsz 512`, static batch = fully deterministic), against our sglang stack in the same session:

| engine | greedy MATH500 | eod rate | mean gen tokens |
|---|---|---|---|
| albatross original (`bsz512` static) | **199/500 = 39.8%** | 86.2% | 580.8 |
| ours (sglang full stack, conc 64) | **194/500 = 38.8%** | 86.6% | 582.0 |

Per-problem comparison (keyed on problem text): **verdict agreement 487/500 = 97.4%**;
**completion text byte-identical on 374/500 = 74.8%** of problems (~580-token generations,
two entirely different engines); flips are near-symmetric (albatross-only-correct 9,
ours-only-correct 4 — the 5-problem score gap). For scale: our own engine's same-config
run-to-run identity is 446/500 (Addendum 1), so cross-engine 74.8% text identity plus 97.4%
verdict agreement is exactly the ulp-noise-amplification picture, with zero evidence of a
systematic accuracy gap (1.0pt << binomial 1σ ≈ 2.2pt). Artifacts:
`bench/results/math500_greedy_albatross_original.json` (summary; original run config embedded),
`bench/results/math500_greedy_crosscheck_ours{,_generations}.json*`.

## Cross-references
[[F0007]] (albatross speed baseline) · [[F0023]] (kernel audit; why kernels don't move accuracy) ·
`bench/math500_avg64.py` · `bench/results/math500_avg64_1.5b.json`.
