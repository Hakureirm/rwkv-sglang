# 基准测试全景

本项目所有测过的指标,人类可读版。每张表写明测试条件,并链接已入库的原始输出。
方法学细节和负面结果在 [`findings/`](findings/) 的编号报告里;本页是可以直接读的汇总。

**模型。**所有权重都是 RWKV-7(RWKV7-G1 系列)。**除非某表/某行另有说明,模型即 1.5B**
(`rwkv7-1.5b-fla`,量化行为其预量化权重)。**0.1B**(`rwkv7-0.1b-fla`)和 **7.2B**
(`rwkv7-7.2b-fla`)行总是标注其尺寸。精度除非行内注明量化档(w8g64 / w8a8 / int4-GPTQ / w4),
否则为 fp16/bf16。

**引擎版本。**自 2026-07-05 起,所有新测量在 **sglang main** 上进行;更早的数字测于
v0.5.10,保留并标注"(v0.5.10)"。两者都有的地方并排展示——版本切换经过完整验证,
正确性没有任何变化,3090 反而各项都略快了。

**两种计时方式**(不同计时方式的数字不能互相比较):

| | 稳态 | 全程 |
|---|---|---|
| 白话含义 | "跑起来以后每秒吐多少字" | "从发出请求到收完全文,平均每秒多少字" |
| 是否包含读题时间(首字延迟)? | 不含 | 含 |
| 工具 | `bench/serving_scale.py` | `bench/bsz_throughput.py`(64 进/256 出) |
| 用于 | §3 单请求阶梯、§4 量化 | §5 并发扫描、§6 全卡、§7 Albatross 对比 |

同一配置同一张卡,稳态读数比全程高约 3%。下面每张表都注明用的是哪种。

所有对比表都用 **cuda-graph 开启**(生产解码路径——serving_scale.py、run_clean_comparison.py、
以及经 serve.sh 的 bsz_throughput.py)。另有一个脚本 throughput.py 报 **eager(cuda-graph 关闭)
基线**,仅供内部内核开发追踪;它的批量解码数字约低 2 倍,从不与 cuda-graph 表格混用。

---

## 1. 正确性(其余一切的地基)

贪心解码与一个纯 numpy fp32 参考实现(`bench/oracle_numpy.py`)逐 token 比对。
任何配置必须 24/24 全对才算通过。

| 项目 | 结果 | 原始件 |
|---|---|---|
| 0.1B / 1.5B / 7.2B,CUDA,fp16+bf16 | 24/24 精确;sglang main 与 v0.5.10、RTX 3090 与 RTX 5090 都验证过 | `bench/results/greedy_gates_5090.log` 等 |
| 动态批 / 分块预填充 / CUDA graph | 精确(含混合批、共享前缀场景) | F0022 |
| int8(w8g64)| 24/24 精确——量化对贪心输出无损;已在 sglang main 上复验(RTX 5090,fp16) | §4、F0015、`bench/results/quant_oracle_gates_5090main.log` |
| TP 2/4/8、PP 2/4/8、tp2×pp2 | 真多卡 24/24 精确;**main 上 cuda-graph 开启时,TP=2 与 PP=2 均贪心 24/24 == 单卡且确定性**(2×L4;修复了一个 PP+cuda-graph 的 capture 崩溃——F0036) | F0019、F0036 |
| Apple Silicon(MLX),0.1B + 1.5B | 24/24 精确,普通算子路径和自写 Metal 核路径都过 | [`../mlx_port/`](../mlx_port/) |
| 批内位置无关性 | 一条请求单独跑和混在批里跑,输出一致(前 4 个 token 有硬保证;更长处只剩 bf16 累加误差,任何引擎皆然) | test/registered/models/test_rwkv7.py |
| 防漂移 | 每次大改后重跑压缩率:最近一次全量重跑**逐位一致**(0.6085,约 750 万 token 上漂移为 −0.0000) | F0024 附录 |

## 2. 精度指标(RWKV 官方指定的两把尺)

**压缩率**(对新鲜语料的每字节比特数,越低越好;与分词器无关;15 类语料 × 500 篇)。
**2026-07-05 已在 sglang main 上全量重测:下表每个值在 RTX 5090 和 RTX 3090 上都复现到小数点后第四位**
——不同芯片、不同引擎版本,约 750 万 token 的池化交叉熵得到同样的结果(原始件
`bench/results/uncheatable_full_*` 两套):

| 模型 · 精度 | 压缩率 | 与 fp16 之差 |
|---|---|---|
| 1.5B fp16 | **0.6085** | — |
| 1.5B int8 w8g64 | 0.6086 | +0.0001(实际无损) |
| 1.5B int8 w8a8(高吞吐路径) | 0.6161 | +0.0076 |
| 1.5B int4 GPTQ | 0.6514 | +0.0429 |
| 7.2B fp16 | **0.5413** | 比 1.5B fp16 低 0.0672(模型越大压得越好) |
| 7.2B int8 w8a8 | 0.5454 | +0.0041 对 7.2B fp16 |
| 7.2B int4 w4(rwkv_w4,g64) | 0.5615 | +0.0202 对 7.2B fp16 |

**量化在 7.2B 上代价更小。** w8a8:+0.0041(7.2B)对 +0.0076(1.5B);int4:+0.0202(7.2B,普通 RTN
`rwkv_w4`)对 +0.0429(1.5B,更强的 GPTQ)——7.2B 的 RTN 权重即便用更弱的量化器,退化也不到 1.5B GPTQ
的一半,说明大模型显著更能吸收低比特权重(`bench/results/uncheatable_full_{w4,w8a8}_7.2b_5090main.json`)。

**非对称(scale+zero)GPTQ 对 int4 的改良——零核改动、代价便宜(F0043,2026-07-07)。**
在同一份语料(N=300,历史 N=7500 语料已不在盘上,两者不可直接比,仅 fp16 本身在两个 N 下就差
~3%)上做的干净三方重测:

| 1.5B · N=300 | 压缩率 | 与 fp16 之差 |
|---|---|---|
| fp16 | 0.5893 | — |
| int4 GPTQ,对称 | 0.6330 | +0.0437 |
| int4 GPTQ,**非对称** | **0.6186** | +0.0293(补掉对称缺口的 33%) |

核完全不用改:int4 解包本来就用满两补码全范围 `[-8,7]`,对称方案只用了 `[-7,7]`——把
`q_unsigned - 8` 存进去,核的现有输出就已经精确到一个逐组常数的偏差,补一次
`groupsum_x @ bias.T`(O(N·K/64),最宽投影额外算力 ≤1.6%)就能精确修正(`bench/verify_w4.py`
12 组配置验证,误差 7.6e-5~2.2e-4,与核自身噪声地板同量级)。§4 和 F0043 有完整的 MATH500
推理塌方画像——非对称没能完全解决这个,以及为什么暂不推荐上马 Stage 2(真 K-quant 混合精度)。

15 类逐项明细(全列,不挑好看的)和位置曲线(证明循环状态在持续吸收上下文:开头 3.65
bits,1024 token 之后降到 2.24 bits)见 `bench/results/uncheatable_*` 与对应报告。

**MATH500**(忠实移植 Albatross 的 `eval_math500.py`:同样的提示词、采样参数、判分器、
1500 token 预算):

所有行为 **RTX 5090**,除非注明(avg@64 另有 3090 列);"main"/"(v0.5.10)"为引擎版本。

| 模型 · 精度 · 设置 | 值 | 说明 |
|---|---|---|
| 1.5B fp16 avg@64(v0.5.10) | **0.4060**(12,991 / 32,000 次生成) | 500 题 × 每题 64 次 |
| 1.5B fp16 pass@64(v0.5.10) | 0.6980 | 64 次里至少对一次的题占比 |
| 1.5B fp16 贪心 avg@1(v0.5.10) | 0.3920(196/500) | 确定性解码 |
| 1.5B fp16 贪心 avg@1(main) | **0.3940(197/500)** | 差 +0.0020,远小于 ±0.0220 的统计波动 → 精度没有变化(`bench/results/math500_greedy_5090main.json`) |
| 1.5B fp16 avg@64(main) | **0.4042**(RTX 5090)/ **0.4063**(RTX 3090) | 两张卡都落在旧版 0.4060 的 ±0.0027 单跑波动带内 → 两张卡都无精度回归(原始件 `math500_avg64_{5090main,3090main}.json`) |
| **7.2B** fp16 贪心 avg@1(main) | **0.6320(316/500)** | 旗舰:比 1.5B 的 0.3940 高 23.8pt——推理强得多,5090 上 3,248 tok/s(`math500_greedy_7.2b_5090main.json`) |
| 1.5B **w8a8** avg@64(main) | **0.3812**(12,197/32,000) | 对 1.5B fp16 0.4042 = **−2.3pt**——低方差尺量出的真实 int8 推理损失;压缩率(0.6161)和 greedy 都藏住了(`math500_avg64_w8a8_5090main.json`) |
| 1.5B **w8a8** 贪心 avg@1(main) | 0.3800(190/500) | 对 1.5B fp16 0.3940 = −1.4pt(n=500 下在 1 个二项标准误内) |
| 1.5B **int4 GPTQ** 贪心 avg@1(main) | 0.1560(78/500) | 对 1.5B fp16 0.3940 = **−24pt 塌方**——困惑度类尺严重低估 int4 对推理的伤害(见 §4 警告) |
| 1.5B **int4 GPTQ,对称** avg@64(main,3090) | **0.1498**(4,794/32,000) | 对 fp16 avg@64 0.4060 = **−25.6pt**;证实贪心塌方(0.1560)不是解码策略偶然产物——64 次温度采样下量级一致。截断率 57.7%(fp16 14.2%),平均生成 token 1022.9(fp16 581)——"跟丢后停不下来",不是"自信答错" |
| 1.5B **int4 GPTQ,非对称** avg@64(main,3090,新增 2026-07-07) | **0.2199**(7,036/32,000) | 对对称 0.1498 有真实改善(离 fp16 缺口收窄 27.4%,截断率降到 30.5%),来自零核改动的修正(F0043),但离 fp16 仍有巨大差距。这个收窄比例比同一修正在 lambada(35.0%)或压缩率(32.9%)上的收窄都小——指标越关键,单靠加大 scale/zero 编码空间能买回的就越少,是"这不是单纯位数不够"的证据 |

三档量化在**推理尺**上按伤害排序:w8g64(仅权重,贪心无损)→ w8a8(−2.3pt)→ int4(对称或非对称
都是 −24 到 −26pt)。只看压缩率会排成 +0.0001 / +0.0076 / +0.0293–0.0429——顺序一样但把 int4
的塌方严重低估了,所以 MATH500 才是这里判定量化质量的那把尺。

## 3. 单请求速度阶梯(稳态,1.5B fp16)

每行在上一行基础上多开一组手写核。RTX 5090 为 sglang main 实测;3090 列是 v0.5.10
的历史阶梯,供对照。

| 配置 | RTX 3090(v0.5.10) | RTX 5090(main) | 5090 相对自身基线 |
|---|---|---|---|
| 不开任何加速核 | 166.5 | 261.9 | — |
| + 融合 GEMV + 稀疏 FFN | 202.9 | 311.1 | +18.8% |
| + 融合 LoRA 链 | 226.5 | 341.6 | +30.4% |
| **+ 融合 token-shift 胶水 + 启动参数自调优(全开)** | — | **409.8** | **+56.5%** |
| int8 w8g64(预量化) | 227.4 | **461.9** | +76.4% |
| int4(预量化) | 259.1 | **548.8** | +109.5% |

原始件:`bench/results/ladder_*_5090.log`。3090 在 main 上的阶梯正在重测。

## 4. 量化(付出什么,得到什么)

三种模式,都配手写 CUDA 核,都能按 GPU 架构即时编译:

| 模式 | 精度代价 | 优势场景 | 权重下载 |
|---|---|---|---|
| int8 w8g64(仅量化权重) | 测不出来(贪心精确;压缩率 +0.0001) | 小批量提速(5090 上单请求比 fp16 全开快 13%)+ 权重体积减半 | ModelScope `Hakureirm/rwkv7-g1-1.5b-w8g64` |
| int8 w8a8(tensor-core) | 压缩率 0.6161(+0.0076,与 cutlass 逐位一致);**MATH500 avg@64 0.3812 对 fp16 0.4042 = −2.3pt**(低方差尺量出压缩率和 greedy 藏住的真实推理损失——与 int4 同类、但轻得多) | 大批量吞吐之王,sm80–90 走上游 cutlass(3090 峰值 9,851 tok/s,64 进/256 出)。Blackwell/sm120 上游没有这个算子;rwkv-sglang 自写的 s8-wmma 核(寄存器分块 V2,位精确门、批不变)已让该档在 5090 端到端可用——int8 GEMM 本身在 M≥512 已快过 fp16 cuBLAS(1.03–1.55 倍),端到端峰值 20,991 tok/s(512 并发,64 进/256 出)= fp16 的 0.9466 倍(剩余差距是逐投影激活量化的 launch 开销,对手是已高度优化的 fp16 基线) | 可联系获取 |
| int4 GPTQ | 7.2B 上 lambada 降 1.28 个百分点(普通 RTN 量化会降 2.64);**1.5B 的 MATH500 avg@64 无论对称/非对称都会塌方(对 fp16 −25.6pt / −18.6pt,见下文)** | 显存最省:7.2B 权重只有 4.6 GB,16 GB 的 T4 上能以 32.9 tok/s 服务 | ModelScope `Hakureirm/rwkv7-g1-{1.5b,7.2b}-w4gptq` |

量化模式需要预量化权重目录(加载器读取 qweight/scale 键;把量化开关指向 fp16 目录会
直接报错,这是有意设计)。

**int8 决定性的地方——单张 32GB 5090 上跑 7.2B(2026-07-06 实测;fp16 一侧已于
2026-07-07 修正,见 [F0047](docs/findings/0047-fp16-72b-concurrency-correction.md))。**
RWKV-7 状态恒定大小,所以 state pool 的槽位数就是最大并发(每请求状态约 33MB,两者
相同——它是 fp32 模型状态,与权重量化无关)。

**修正说明:** 原 fp16 扫描网格只测到并发 221 就停了,依据是一个从未验证过的假设——
"fp16 在那里已经撞到硬性 OOM 上限"。用更细的网格把 221 以上重新测一遍(同一启动配置,
`--mem-fraction-static 0.85`)发现吞吐其实还在非单调地继续爬升,远没有到顶:fp16 真实
的安全运行上限是**至少 344 并发**(368 能干净启动,但真实请求负载下会 OOM——是 eager
prefill 路径上余量过薄导致的分配器碎片问题,不是 221 处的硬性架构上限),**真实峰值是
6,709 tok/s @ c320**(c336 已确认回落到 6,039,c344 为 6,111)——不是 5,983 @ c192。不过
手写核栈确实比 bf16 stock 路径多占真实显存:同样 `--cuda-graph-max-bs 320` 下,fp16 全核栈
cuda-graph capture 完成后只剩约 2.0GB 余量,而 bf16 stock 路径同配置下有约 6.4GB(权重、
state pool 算法完全相同,只是核栈不同)——所以 fp16 确实无法把并发天花板推得和 bf16 一样
远。但它更快的单步算力足够把这点补回来:fp16 修正后的峰值(6,709 @ c320)仍比 bf16 自己
的峰值(6,171 @ c256,见 §12)高 8.7%,手写核依然是端到端净赢,只是在高并发尾部的优势比
bsz1 时窄一些。同一启动配置、cuda-graph 开、64 进/256 出:

| 单卡 7.2B | 最大并发 | 峰值输出吞吐 |
|---|---|---|
| fp16 | **≥344**(原先误报为 221) | **6,709 tok/s @c320**(原先误报为 5,983 @c192) |
| **w8a8** | **640(1.86×)** | **7,587 tok/s @c640(1.131×,到 640 仍在爬)** |

并发全曲线(输出 tok/s)——fp16 修正后的曲线在 320 见顶后回落(未崩溃);w8a8 靠更大的
显存预算继续爬:

| 并发 | 1 | 128 | 192 | 221 | 256 | 320 | 336 | 344 | 368 |
|---|---|---|---|---|---|---|---|---|---|
| fp16(已修正) | 124 | 5,688–5,695 | 5,999–6,034 | 5,769–5,772 | 6,186–6,205 | **6,709–6,714(峰)** | 6,039 | 6,111 | 真实负载下 OOM |
| **w8a8**(未变) | 60 | 4,657 | — | 5,342 | — | 6,304 | — | — | — |

(fp16 区间取自两次独立复测,`--cuda-graph-max-bs` 分别为 320 与 344,重叠点差异均
<0.5%:`bench/results/72b/sweep_72b_fp16_v2_5090.json` / `sweep_72b_fp16_v3_5090.json`;
原粗网格文件 `sweep_72b_fp16.json` 保留作过程记录,不再是当前结论来源。)w8a8 的曲线到
640 仍在上升(撞的是它自己的显存天花板:state pool 20.03GB、剩 1.92GB),所以 7,587 是
显存受限的下限、不是算力见顶。即 int8 仍然让 7.2B 跑到真实的 **1.86× 并发、比 fp16 高
13.1% 峰值**(相比之前发布的 2.90×/26.8× 下修——是 fp16 一侧被网格测试不足所低估,
w8a8 一侧数字未变)——fp16 依然被钉在显存上限附近,只是没有之前认为的那么紧。诚实机制:
相同并发≤221 时 fp16 每步更快(不付激活量化开销);w8a8 靠跑到 fp16 无法安全维持的并发
反超。原始件:`bench/results/72b/`。

**一个诚实的 int4 警告(2026-07-05 实测,2026-07-07 补上非对称+avg@64,F0043):**
困惑度类指标会低估 int4 对多步推理的伤害。1.5B 的 GPTQ 权重上,压缩率看起来损失不大
(0.6514 旧 N=7500 / 0.6330 干净 N=300 对称版),但 **MATH500 avg@64 是 0.1498**
(32,000 次生成、64 次温度采样——证实贪心读数 0.1560 不是解码策略的偶然产物)**对 fp16
的 0.4060**——量化后的模型推理中途跟丢,越写越长直到撞上 token 上限(截断率 57.7%,
fp16 只有 14.2%,平均生成 1023 vs 581 个 token)。

**非对称(scale+zero)量化——先试的便宜方案,确实有用但没解决问题:** avg@64 改善到
**0.2199**(截断率降到 30.5%),补掉 27.4% 的 fp16 缺口。这个收窄比例比同一修正在
lambada(35.0%)或压缩率(32.9%)上的收窄都要小——指标越关键,单靠更宽但仍是 4-bit 的
编码空间能买回的就越少,读作"这更像长链推理上的误差累积,不是单纯位数不够"的证据。
**判决(F0043):暂不推荐上马完整的 K-quant 混合精度重写(Stage 2,Q4_K_M 式 4/6-bit 混合
分块)**——更便宜、信息量更大的下一步是先看 7.2B 的 int4 MATH500 是否同样塌方(至今只测过
lambada,损失小得多,−1.28pt 对 1.5B 的 −3.34pt,符合"大模型量化更稳"的规律);如果 7.2B
扛得住,int4 的诚实定位就是"7.2B+ 好用的省显存工具,1.5B 推理类任务用 int8",完全不需要
做 Stage 2。1.5B int4(对称或非对称)都应当作"非推理类任务的省显存工具"使用,不是通用
无损档——w8g64(int8,仅权重)才是无损量化档。原始件:
`bench/results/math500_greedy_w4gptq_5090main.json`(贪心)、
`docs/findings/0043-w4-asym-gptq.md`(完整 avg@64+压缩率+lambada 画像)。

**sm120 w8a8 内核(GEMM 微基准)。**上游 cutlass `int8_scaled_mm` 在 sm120 上编译不了,
所以 Blackwell 消费卡上我们自写的 s8-wmma GEMM(寄存器分块"V2",对逐行参考位精确、批不变)
是唯一的 int8 路。它在 decode/prefill 批量的投影形状上快过 fp16 cuBLAS(RTX 5090,单核 GEMM,
× = 相对 fp16 的加速):

| 投影形状 | M=512 | M=1024 | M=4096 |
|---|---|---|---|
| attn 2048×2048 | 1.08× | 1.33× | 1.52× |
| ffn.k 8192×2048 | 1.45× | 1.52× | 1.55× |
| ffn.v 2048×8192 | 1.03× | 1.28× | 1.53× |

GEMM 赢了,但 1.5B 端到端是 fp16 的 0.9466×(§5):逐 token 的激活量化 launch 无法在每步
约 144 个异构 decode 核间摊销,加上对手是已极致的 fp16 基线,吃掉了内核的余量。这笔税在上面
显存受限的 7.2B 场景里不显(int8 真正的赢——2.90× 并发——就在那里)。原始件:
`bench/verify_w8a8.py --bench`。

## 5. 服务吞吐(RWKV-7 1.5B,全程计时,64 进/256 出,并发扫描)

| 配置 | RTX 3090 main | RTX 5090 main |
|---|---|---|
| 未加速 fp16,单请求 | 153.7 | 256.8 |
| 未加速 fp16,峰值 | 7,205.5(384 并发) | 22,090.8(512 并发) |
| 手写核全开,单请求 | 230.7 | 397.3 |
| 手写核全开,峰值 | 7,257.7(384 并发) | **22,175.3(512 并发)** |
| int8 w8a8 + 融合胶水,峰值 | **9,850.9(256 并发)** | 20,991(512 并发;自写 s8-wmma 核 V2,= fp16 的 0.9466 倍——GEMM 已快过 fp16,端到端略低) |

v0.5.10 参考:3090 未加速峰值当时是 6,885,w8a8+胶水是 9,686——仅仅换到 main 就让
3090 变快了。原始件:`bench/results/bsz_sweep_*_{3090main,5090}.json`。

一个在 main 上依然存在的坑:sglang 对这类模型默认把 `cuda_graph_max_bs` 定为 24,
超过就静默退回慢路径——务必显式设置(serve.sh 已带)。

## 6. 全卡覆盖(同一份代码、同一套配方,每张卡)

1.5B fp16 手写核全开,sglang main,全程计时。**单请求=bsz1 稳态解码;峰值=64 进/256 出
并发扫描的最佳总吞吐**(舰队卡并发封顶 384,工作站 5090 封顶 512):

| GPU | 架构 | 单请求 | 峰值 |
|---|---|---|---|
| T4 | sm75 | 97.1 | 3,176 |
| L4 | sm89 | 102.2 | 4,674 |
| A10G | sm86 | 168.3 | 6,627 |
| A100-40GB | sm80 | 257.0 | 17,042 |
| A100-80GB | sm80 | 278.9 | 18,420 |
| L40S | sm89 | 238.0 | 13,352 |
| H100 | sm90 | 361.1 | 28,578 |
| H200 | sm90 | 399.3 | 32,289 |
| B200 | sm100 | 381.6 | **40,544** |
| RTX PRO 6000 | sm120 | 315.0 | 21,566 |
| RTX 5090(工作站) | sm120 | **397.3** | 22,175 |

值得注意:单请求速度上,消费级的 5090 追平 H200(397.3 对 399.3)、超过 H100 和
B200——单流解码拼的是显存带宽,GDDR7 不输 HBM。原始件:
`bench/results/fleet_main_10cards.json`。

## 6b. 多卡:张量并行 / 流水并行(main 上验证,cuda-graph 开)

张量并行与流水并行,在 sglang main 的生产 cuda-graph 路径下测(F0019 的矩阵是 cuda-graph 关)。
1.5B bf16,2×L4,全程计时,**64 进/256 出**(c1/c8/c32/c64=并发)。**tp=2 与 pp=2 均贪心 24/24 == 单卡且确定性**——多卡不改变输出。
(pp 在这里能跑通,先要修一个 cuda-graph capture 崩溃——F0036。)

| 配置 | 贪心 vs 单卡 | c1 | c8 | c32 | c64(峰值) | vs tp=1 |
|---|---|---|---|---|---|---|
| tp=1(1 卡) | 参考 | 72.6 | 482.3 | 1,612.9 | 2,582.6 | — |
| **tp=2** | **24/24 精确** | 105.3 | 655.9 | 2,008.6 | **3,026.2** | **1.17×** |
| **pp=2** | **24/24 精确** | 65.4 | 367.7 | 1,365.5 | 2,288.8 | 0.89× |

诚实读:1.5B 在 PCIe 连接的 L4 上,TP=2 在 c64 给 ~1.17×;PP=2 是 0.89×(此规模流水气泡占主导——
PP 的职责是装下超过单卡的模型,不是给小模型每 token 提速)。价值在于两者在 main 上都**正确且可
生产**;真正需要多卡的模型(7.2B+、NVLink)上的扩展曲线是后续工作。原始件:`bench/results/tppp_l4_main.json`。

## 7. 与 Albatross 对比(BlinkDL 官方速度参照)

Albatross 是一个纯前向测速程序(没有请求调度、动态批和服务接口),所以这个对比只回答
一个问题:单流裸速度差多少。每张卡用同一份 1.5B 权重文件。它的出厂参数是作者在自己的
RTX 5090 上调的——在 5090 上是它的最佳状态,在其他卡上就是用户拿到手的真实状态。
计时说明:Albatross 列不含读题时间,我们这列含(约压低我们 3%),所以下面的比值只会
低估我们,可以放心引用。

| GPU | Albatross(tok/s) | 我们(tok/s) | 我们 / Albatross |
|---|---|---|---|
| T4 | **stock 核编译不过**(sm80+ `cp.async`;可移除——见注†) | 97.1 | 开箱即跑,只我们 |
| L4 | 113.5 | 102.2 | **0.9004** |
| A10G | 203.4 | 168.3 | 0.8274 |
| L40S | 291.8 | 238.0 | 0.8156 |
| A100-40GB | 341.3 | 257.0 | 0.7530 |
| A100-80GB | 385.5 | 278.9 | 0.7235 |
| RTX 3090 | 309.2(我们替它为这张卡重调过参数) | 230.7 | 0.7461 |
| RTX PRO 6000 | 457.4 | 315.0 | 0.6887 |
| RTX 5090(作者自己的调参卡) | 553.9 | 397.3 | 0.7173 |
| H100 | 607.3 | 361.1 | 0.5946 |
| H200 | 684.3 | 399.3 | 0.5835 |
| B200 | 744.0 | 381.6 | 0.5129 |

† T4 这条差距是 Albatross **出厂** WKV 核里的 `cp.async`(sm80+ 指令);BlinkDL 指出这是可移除的
——改核后能在 T4 跑,所以是打包限制而非本质限制。这里的说法严格限定「开箱即用」:我们的栈不改
就能服务 T4,而我们没有基准测过手改后的 Albatross 在 T4 上的数字。

怎么读:差距大小跟显存带宽正相关。推理卡上我们很接近(L4 上 0.9004);带宽极高的卡上
它的整层融合设计越占便宜(B200 上 0.5129),因为我们按算子拆分的实现,每次启动计算核
的固定开销在快卡上占比变大——这正是我们下一步(CUDA graph 消除启动开销、加深融合)的
方向。另外两个事实:我们的 **int4 在作者自己的 5090 上达到它 fp16 速度的 0.9908 倍**
(548.8 对 553.9,跨精度);T4 那行显示了覆盖面的差别。原始件:
`bench/results/albatross_fleet_10cards.json`。

还有一个发现:换到 CUDA 12.9 后,Albatross 出厂参数连在它调参用的 5090 上都不再是
最优。我们更进一步,**亲手替 Albatross 为这张卡重新调参**(14 处分发表修改,每处都做了
数值与端到端双重验证;抓到并回退了一个微基准假赢面——小权重在缓存里跑赢、真模型冷读时
反而变慢;完整 diff 与证据在 `bench/results/albatross_5090/`)。出厂 → 重调后的提升
(RTX 5090,三跑取中位):

| 模型 | 批量 1 / 8 / 32 | 解码 | 读题 |
|---|---|---|---|
| 0.1b | | +0.0% / **+11.0%** / +0.0% | +0.9% / +2.2% / **+13.4%** |
| 1.5b | | +0.0% / **+6.6%** / **+7.9%** | +5.2% / +1.9% / +2.9% |
| 7.2b | | +0.0% / +3.5% / +0.0% | +1.2% / +2.9% / **+5.0%** |

单流解码纹丝不动(显存带宽墙;出厂状态的 7.2b 147.0 tok/s 本来就超过作者自己发布的
144.04),批量场景最多追回 13%。上面的单请求比值以出厂值为基准;换成重调后的值,bsz1
处的比值不变(554.0 对 553.9)。我们的启动参数在任何卡、任何 CUDA 版本上都是运行时
现场重选的——这就是下一节量化展示的设计差异。

**带宽差距的进行中工作(2026-07-07,F0051)。** 上表是默认配置(所有融合开关关闭)。
在 H100 上做了一次真实的核 launch 画像(每个完整 decode 步 699 次 launch,不是早前
内部估计的~144——用 `torch.profiler` 重新实测,别信旧数字),找到 launch 数最高的
还未融合核簇(LoRA-output gate 数学:3 个 sigmoid + 周边 elementwise)并融合成一个核,
sm_89 与 sm_90 双架构位精确(`max_abs_diff = 0.0`),开关 `RWKV_FUSED_GATES` 默认关、
纯新增、不影响上表数字。实测效果(用 bsz8 对照组验证——该配置融合不生效,两张卡都
~0%变化,证明 A/B 隔离干净):**H100 bsz1 359.4→392.6(+9.24%,对 Albatross 比值
0.592×→0.646×);L4 bsz1 82.1→83.1(+1.22%)**——同一份代码改动在 H100 上收益大得多,
本身就是"差距确实与带宽相关"的证据,不只是一个没验证过成因的观察。同一轮画像给出的
诚实上限:即便把剩下所有小 elementwise 核都这样融合,H100 也只能到约 0.69×——更大的
剩余部分是我们的 GEMV 核把 epilogue 当独立 launch 跑,而 Albatross 的巨核把它们直接
内联;那项融合(爆炸半径更高——牵动每个量化档都依赖的 GEMV 核)正在进行。见
[F0051](findings/0051-lora-gate-fusion-highbw.md) 及该序列后续 finding 了解最新状态;
这明确是长期迭代项(task #5),不指望完全补平。

**更新(2026-07-07,F0052)。** 落地并实测了上面提到的第一个"把 elementwise 融进 GEMV
epilogue"候选项:把 FFN 的 `relu(key(xk))**2` 激活(2 次 elementwise launch)直接折进
`ffn.key` 这次 GEMV 自己的写回里,而不是在它之后单独跑 2 个核。sm_89 与 sm_90 上位精确
(`torch.equal`),并验证了量化档(w4、w8a8)在开关两侧位一致、完全不受影响。实测效果
(隔离方法同上):**H100 bsz1 393.2→404.3(+2.82%,独立复测 +2.72%;对 Albatross 比值约
0.65×→0.66×);L4 bsz1 83.1→83.3(+0.24%,噪声量级)**——高带宽卡上是真实收益,低带宽卡
上基本无感,与 F0051 同一条带宽相关性规律再次成立,但收益比 LoRA-gate 融合小得多(与它
折叠的 launch 簇更小成正比,2 次 vs ~7-8 次)。核级别的账目显示融合后的 epilogue 本身几乎
不额外占用 GPU 时间,因此**把上面的上限估计从约 0.69× 上修到约 0.71×**。开关
`RWKV_FUSED_SQRELU` 默认关、纯新增。见 [F0052](findings/0052-sqrelu-epilogue-fusion-highbw.md)。

## 7b. 与 vllm-rwkv(社区 vLLM fork)对比

2026-07-06 在严格同等条件下实测,**RWKV-7 1.5B**:同两张卡(RTX 3090 + RTX 5090)、同一份权重文件(1.5B)
(tensor 级验证)、同一套并发扫描客户端(vllm-rwkv 走其 OpenAI 接口,协议完全一致:
64 进/256 出)、vllm-rwkv 用其文档标注的最优配置。须声明:vllm-rwkv 当前版本
(`4bf0239a1`)出厂状态在第一次解码即崩溃(自动上游同步引入的接口不匹配),下表 vllm-rwkv
的全部数字均在补一个 2 行兼容修复后测得。该分支每天强制变基——复现时务必钉住 commit。

**正确性:**vllm-rwkv 的 fp16 引擎同样在两张卡上 24/24 逐 token 复现了 numpy fp32
参考序列——两个独立引擎收敛到同一个参考实现,是互相印证,如实记录。

**吞吐,vllm-rwkv / rwkv-sglang 全栈(全程计时,64 进/256 出;>1 = vllm-rwkv 更快):**

| 并发 | RTX 5090 | RTX 3090 |
|---|---|---|
| 1 | 1.1352(vllm-rwkv 领先) | **0.8114(sglang 领先:230.7 对 187.2)** |
| 8 | **0.9204(sglang 领先)** | **0.8947(sglang 领先)** |
| 32 | **0.9866** | **0.9980** |
| 64 | **0.9858** | **0.9557** |
| 128 | 1.0507 | **0.9745** |
| 256 | 1.2194 | 1.0985 |
| 峰值(512/384) | 1.2621(27,988 对 22,175) | 1.1702(8,493 对 7,258) |

**诚实读法:**vllm-rwkv 的计算核就是 Albatross 的(逐文件移植),所以单流跟着 Albatross
的底子走——5090 上 vllm-rwkv 领先;3090 上 rwkv-sglang 的手写核反超其移植版。5090 中段
(并发 8–64)rwkv-sglang 领先。**两张卡的高并发都是 vllm-rwkv 领先(最高 1.26 倍)——这是本次对比
最真实的结果,也是 rwkv-sglang 下一轮内核工作的靶子。**两个应对已在手:3090 上
rwkv-sglang 的 int8 峰值(9,851)反超 vllm-rwkv 的 fp16 峰值(8,583)达 **1.1477 倍**;
5090 上游没有 int8 算子,rwkv-sglang 自写的 s8-wmma 核(V2)已把该档跑通(20,991 @512 并发
= fp16 的 0.9466 倍;int8 GEMM 本身 M≥512 已 1.03–1.55 倍于 fp16)——可用性缺口已补上;
1.5B 上端到端略低于自家高度优化的 fp16,int8 的决定性优势在 7.2B(显存受限档)。原始件:`bench/results/vllmrwkv/`。


> 3090 这一列是干净重测(`_v2`,max_num_seqs 按 24GB 配足):每一档都在首轮箱测的 ±2%
> 以内(若有偏差是略低),说明之前那组数字站得住——3090/5090 的不对称是真实硬件效应
> (带宽越高,融合层内核在高并发越占便宜),不是配置假象。

## 7c. 真实负载对比(ShareGPT 变长对话)

上面的合成扫用的是单一固定形状(64 进/256 出)。真实服务是变长的,对调度器的压力不同。**本节 RWKV-7 1.5B。**
同一个中立客户端(`sglang.bench_serving`)、同一份 ShareGPT 文件、同 500 条请求、同权重,
两引擎各用最优配置。两档负载:峰值(全部请求一次涌入)和稳态(16 req/s)。同等条件的铁证:8 组运行处理的
输入 token 完全相同(168,913 个),生成的输出 token 也完全相同(109,861 个)——同样的
输入进、同样的 token 出(权重相等 + 贪心 + ignore_eos)。

**输出吞吐(tok/s)与延迟,RTX 5090:**

| 负载 | 引擎 | 输出 tok/s | 首字延迟中位 | 每字间隔 p99 |
|---|---|---|---|---|
| 峰值 | rwkv-sglang | **9,602** | **2,503 ms** | **20.5 ms** |
| 峰值 | vllm-rwkv | 8,865 | 3,458 ms | 370.8 ms |
| 16 req/s | rwkv-sglang | 3,300 | 31.6 ms | 37.8 ms |
| 16 req/s | vllm-rwkv | 3,351 | 24.1 ms | 22.7 ms |

**RTX 3090:**

| 负载 | 引擎 | 输出 tok/s | 首字延迟中位 | 每字间隔 p99 |
|---|---|---|---|---|
| 峰值 | rwkv-sglang | **3,974** | **7,297 ms** | **717 ms** |
| 峰值 | vllm-rwkv | 2,805 | 12,750 ms | 1,595 ms |
| 16 req/s* | rwkv-sglang | 2,477 | **316 ms** | 1,239 ms |
| 16 req/s* | vllm-rwkv | 2,600 | 375 ms | 375 ms |

*3090 在这个模型上其实撑不住 16 req/s(两引擎实测都只到约 11–12 req/s),所以这一行是
轻度过载,不是真正的稳态。5090 可以从容处理 16 req/s。

**结论反转——这正是重点。**在*合成固定形状*扫描上,vllm-rwkv 高并发领先(它的 Albatross
内核 + decode-wave 批处理喜欢整齐的形状)。但在*真实变长*负载的峰值下,**rwkv-sglang 两张卡
的吞吐都领先**(5090 上 1.08 倍、3090 上 1.42 倍),且首字延迟中位更低——sglang 的连续动态
批处理能把长短不一的请求打包得更满,不像 wave 调度器在变长形状上留下空泡。稳态 16 req/s 时
两者吞吐相差几个百分点,尾延迟互有胜负(vllm-rwkv 稳态的每字间隔尾更紧;rwkv-sglang 在 5090
峰值下的尾紧得多)。总结:高负载真实混合长度服务下 rwkv-sglang 领先;轻稳态负载下互有来回。
原始件:`bench/results/realload/`。

## 8. 启动参数自调优逐卡收益(硬编码参数为什么换卡就失效)

我们的 GEMV 启动自调优与内置启发式的核级对比,基于 **RWKV-7 1.5B** 的投影形状(交错 4 遍取中位;默认只调数值安全的
参数轴)。收益 = 该形状上省下的时间:

| GPU | 三个形状的收益 | 一句话 |
|---|---|---|
| T4 | +7.6% / +5.6% / +2.5% | 启发式失手的卡上兑现收益 |
| L4 | +0.1% / +11.3% / **+24.1%** | 最大赢面 |
| A10G | +0.1% / +0.3% / +2.1% | 接近持平 |
| A100-40/80 | 最高 +4.9% | 互有胜负 |
| L40S | +0.0% / +9.2% / +2.6% | 有赢面 |
| H100 / H200 / B200 | ≈ 0 | 启发式已是最优,如实报零 |
| RTX 3090 | 0% ± 噪声 | 诚实的零(服务级 7 次重复) |
| RTX 5090 | +0.0% / +3.2% / +5.0% | 170 个 SM 的卡上最优分块与启发式不同 |

原始件:`bench/results/autotune_ab_9cards.json`、`autotune_ab_5090.json`。方法学
(包括迫使我们改用交错测量的时钟爬坡伪影)见 F0025。

## 9. 真实负载下的延迟

**泊松到达**(RWKV-7 1.5B;请求按固定平均速率随机到达;512 进/256 出;RTX 5090,main):

| 到达速率 | 输出 tok/s | 首字延迟 中位/最慢1% | 每字间隔 中位/最慢1% |
|---|---|---|---|
| 每秒 2 个 | 524 | 23.6 / 43.4 毫秒 | 3.8 / 5.1 毫秒 |
| 每秒 8 个 | 2,047 | 26.6 / 52.2 毫秒 | 5.1 / 5.5 毫秒 |
| 每秒 16 个 | 3,977 | 约 27 / 52 毫秒 | 约 5 / 5.5 毫秒 |
| 300 个同时涌入 | 11,865 | 1.7 / 3.3 秒 | 18.6 / 24.7 毫秒 |

每秒 16 个请求以内完全不排队——首字延迟稳定在 26 毫秒上下。3090(v0.5.10)在同样
16 请求/秒时首字延迟是 302 毫秒。原始件:`bench/results/pd_mixed_5090.json`、
`pd_mixed_3090main.json`。

**ShareGPT 真实对话负载**(RWKV-7 1.5B;标准 `bench_serving`,500 个请求,RTX 5090):峰值输出
9,845.6 tok/s、总吞吐 27,527.7 tok/s;每秒 16 个请求时首字延迟中位数 32.3 毫秒。
原始件:`bench/results/sharegpt_{peak,r16}_5090.log`。

## 10. 结构性优势:状态大小恒定

| 放大的维度 | 基线 | 放大后 | 显存增量 |
|---|---|---|---|
| 并发 1 → 256 路(1.5B,3090) | 12,420 MiB | 12,622 MiB | **+202 MiB** |
| 上下文 1K → 64K(1.5B) | 12,364 MiB | 12,368 MiB | **+4 MiB** |
| 上下文 1K → 32K(7.2B) | 17,866 MiB | 17,866 MiB | **+0 MiB** |
| 并发 1 → 64 路(7.2B,24 GB 卡) | 46.6 tok/s | 1,802.7 tok/s | +308 MiB |

Transformer 的 KV 缓存在这两个维度上都会增长;RWKV-7 的状态不会。这就是**单张 32 GB 5090
用 w8a8 能同时服务 640 路 7.2B 请求**的原因(§4)——随并发增长的只有 state pool,而它每请求
固定且极小。(上表的显存增长行是 v0.5.10 测量;此性质由架构决定,main 上不变。)

## 11. 投机解码

**2026-07-07 更新——正确性已完成,速度是真实的部分进展。** 0.1B RWKV-7 起草 K 个
token;target 通过 sglang main 自身的 spec-V2 插件架构验证(`RWKV_SPEC`,照
`NGRAMWorker` 模板搭建——复用上游 verify+commit 机制而非自建 worker,见
[ADR-0006 的 2026-07-06 转向记录](adr/0006-speculative-decoding.md));被拒绝的部分
靠恢复 O(1) 状态快照回滚。

**正确性:`bench/spec_gate.py` 已 10/10** token 逐位一致(开投机 vs 关投机,贪心),
128 与 256 两种生成长度下都保持,且既有非投机解码回归套件零回归。达成过程中修了两个
RWKV-7 循环状态特有的真 bug(这套机制原本是为 transformer 类后端造的,没遇到过这两个
问题):`intermediate_ssm`/`intermediate_conv_window` 是按请求序号索引而非 pool
slot;RWKV-7 的**第二个**token-shift 状态(FFN 那个,`conv[1]`)从未被通用上游提交
钩子纠正过(该钩子原本只处理 `conv[0]`,因为 GDN/KDA/Lightning 各自只有一个
token-shift 等价状态)。

**速度:给 draft 自己的 decode 循环手搓了一个 CUDA graph**(不是 sglang 共享的
`DecodeCudaGraphRunner`——它硬编码了一个 EAGLE 族的批形状假设,与纯循环 draft 不合),
拿到真实、已过门的**1.5–1.6× draft decode 步加速**(中位数 33.2→53.7 tok/s,7 条
测试 prompt 全部为正)。对比完全不开投机解码的效果:**仍慢 2.6×–4.5×**(开 cuda
graph 前是 7×)——真实、诚实的部分进展,还不是这个功能该有的净胜。已记录三个尚未
攻克的下一步候选瓶颈(每层状态 clone 开销、target-verify 本身开销、每轮 Python
编排开销)。

完整分析:[F0046](findings/0046-spec-decode-strategy-b-build.md)(本次构建)、
[F0031](findings/0031-spec-decode-increment-i.md)(已被取代的 v0.5.10 时代原型,
留作 M-shape 归约顺序那一课的记录)、F0029(可行性,α=0.738–0.7485)、
ADR-0006(设计)。

## 12. Apple Silicon(MLX)

Apple Silicon 原生 RWKV-7——MLX + 自写 Metal WKV 核,用与 CUDA **同一个 numpy fp32 oracle** 把关。
MLX 端**已与 CUDA 平台覆盖对齐**:核 profiling、量化、压缩率尺、真实负载基准——全部在 **Apple M5
(32 GB 统一内存,MLX 0.31.2)** 上。这是台共享机,解码抖动分两种量级:**同一次背靠背会话内**很紧
(~1–5%:2026-07-07 连续跑 10 次,1.5B fp16 解码落在 36.2–36.5 tok/s);但**跨会话**(不同时间/日期,
代码和 `mlx`/`mlx-lm` 版本都相同)时,会话中位数观察到最高 **~12% 峰谷差**——2026-07-06/07 五次独立
1.5B fp16 解码会话横跨 32.8–37.3 tok/s(完整明细见 [F0045](findings/0045-qwen35-mlx-matched-benchmark.md)
的补记)。单次会话的绝对 tok/s 请当作某一时刻的快照、不是恒定常数——下面的量化头条数更可靠,因为它们
用**同进程交错 A/B**(每轮 baseline+变体背靠背测,也能抵消跨会话漂移),`bench_mlx.py` 报中位+最优。

### 12.1 fp16 默认——正确性 + 速度(Metal WKV,bf16 权重)

| | 0.1B | 1.5B | 7.2B |
|---|---|---|---|
| 贪心 vs numpy oracle | 24/24 | 24/24 | 8/8¹ |
| 单流解码(tok/s) | 325.6 | 37.3 | 7.5 |
| 读题 1024 token(tok/s) | 11,486 | 1,905 | 441 |
| 峰值内存 | 0.54 GiB | 3.38 GiB | 14.64 GiB |

¹ 7.2B 的 oracle fixture 是 8 token(0.1B/1.5B 是 24)——三个尺寸在**普通算子和 Metal 两条核路径上都逐
token 精确**;7.2B fp16 装进 32 GB 还有余量。融合 Metal WKV 核为默认(读题比普通算子扫描快 4.4–8.1
倍;`RWKV_MLX_WKV=pure` 为免 JIT 回退)。[F0037](findings/0037-mlx-fused-metal-default.md)、[F0038](findings/0038-mlx-m5-kernel-profiling.md)。

### 12.2 M5 硬件天花板 + 单流时间去向(F0038)

实测 M5:**显存带宽 ~123 GB/s**,矩阵 ~11.4 TFLOP/s @2048² / ~13.2 @4096²。bsz1 解码每 token 把全部
权重读一遍(1.5B ≈ **2.88 GB/token**)→ **硬上限 ~42.7 tok/s**,实测 ~33.6 = **达 79%**。所以解码的
杠杆是*更少的权重字节*(量化,§12.3),而非更多 fp16 核调优(把大投影置零的图内消融让解码 34→400
tok/s——解码*就是*那次权重读)。已落核优化:WKV 扫描里 decay 预计算(`exp` 少 D 倍,**位精确**)→
读题 **+0.8% / +1.8% / +3.1%**(0.1B/1.5B/7.2B);读题 chunk 256 近最优。负结果(记录以免重试):T>1
读题编译 +13% 但破坏 0.1B 位精确 → 回退。

### 12.3 量化——MLX 原生 w8g64 / w4g64(F0039),可选;fp16 仍为精确默认

`RWKV_MLX_QUANT=w8|w4`(`mx.quantize` group-64,对齐 CUDA w8g64/w4-g64;仅权重,bf16 激活):

| 模型 | 模式 | 贪心 vs oracle | 解码 tok/s(中位/最优) | 读题 tok/s | 峰值内存 |
|---|---|---|---:|---:|---:|
| 0.1B | fp16 | 24/24 | 325.6 / 331.9 | 11,486 | 0.54 GiB |
| 0.1B | **w8** | **24/24** | **417.3 / 475.7** | 8,458 | 0.43 GiB |
| 0.1B | w4 | 4/24 | 588.1 / 593.4 | 7,831 | 0.36 GiB |
| 1.5B | fp16 | 24/24 | 37.3 / 39.1 | 1,905 | 3.38 GiB |
| 1.5B | **w8** | **24/24** | **55.5 / 56.0** | 1,908 | 2.28 GiB |
| 1.5B | w4 | 24/24² | 94.0 / 95.8 | 1,975 | 1.65 GiB |
| 7.2B | fp16 | 8/8 | 7.5 / 7.9 | 441 | 14.64 GiB |
| 7.2B | **w8** | **8/8** | **12.6 / 12.9** | 484 | 8.88 GiB |
| 7.2B | w4 | 8/8² | 22.0 / 22.9 | 513 | 5.76 GiB |

- **w8 = 贪心无损,推荐量化档**:解码 **+28% / +49% / +68%**(0.1B/1.5B/7.2B),峰值内存 **−20% /
  −33% / −39%**,贪心输出与 fp32 oracle 完全一致。抵消漂移的 1.5B 交错 A/B:fp16 31.4 → **w8 52.0
  (+66%)** → w4 85.1(+171%)。
- **w4 = 省显存 / 极限解码档**:解码 **+81% / +152% / +193%**,峰值 **−33% / −51% / −61%**(7.2B 只
  要 5.76 GiB),代价是真实精度损失(§12.4)。
- ² w4 在 24/8 token 上的贪心吻合是巧合、非无损(0.1B w4 贪心已发散,4/24)。量化下的读题:0.1B −26%
  (小模型读题算力受限→反量化税),1.5B ~持平,7.2B +10%(足够大,读题也部分受带宽限)。

### 12.4 精度——压缩率(uncheatable,与 CUDA §2 同一尺)(F0040)

直算 harness(`compression_mlx.py`;MLX 无 HTTP 服务),1.5B,15 语料 × 40 文档,pooled bits/byte(越低越好):

| 精度 | pooled bpb | 对 fp16 |
|---|---:|---:|
| fp16 | **0.5926** | — |
| w8 | 0.5929 | **+0.0003(无损)** |
| w4 | 0.6430 | +0.0504 |

与 CUDA 列一致(w8g64 +0.0001,int4 +0.0429):w8 在尺上无损,int4 有真实代价。位置曲线 3.62 → 2.19
bit([0-64)→[1024+))——O(1) 状态持续吸收上下文(CUDA:3.65→2.24)。

### 12.5 真实负载——ShareGPT 单流(F0041)

150 条真实 ShareGPT 对话(取首个人类轮为 prompt;长度 min/p50/mean/max = 8/51/244/1865 token),bsz1
流式,max_new = 128:

| 精度 | TTFT p50 / p90 / p99(ms) | ITL p50 / p90 / p99(ms) | 解码 tok/s | 读题 tok/s |
|---|---|---|---:|---:|
| fp16 | 77.8 / 631 / 1310 | 38.8 / 48.7 / 57.6 | 25.6 | 1,202 |
| **w8** | 71.8 / 608 / 1322 | **19.5 / 27.7 / 37.2** | **48.0** | 1,307 |

w8(贪心 + 压缩率均无损)**把每字间隔砍半**、流式解码近翻倍——推荐的交互默认档。TTFT 随 prompt 线性
(RWKV 固定大小状态),1865 token 长尾也只 ~1.3 s。原始 JSON 见 [`../mlx_port/results/`](../mlx_port/results/);
完整方法学 + 负结果见 F0038–F0041。

### 12.6 CoreML / Apple 神经网络引擎(ANE)——可行性探测:未通过,不报 tok/s(F0042)

第二条 Apple Silicon 路线的探测——CoreML 走 ANE(不是 GPU)——想作为 §12.1 MLX-GPU 数字之外的第三点
(ANE / GPU / CPU)。**可行性门在建完整模型之前就没通过;按本仓"先过 oracle 门再报速度"的纪律,不报
任何 ANE tok/s 数字。**完整方法学、证据与叫停决定见 [F0042](findings/0042-coreml-ane-feasibility.md),
摘要如下:

被测对象 = RWKV-7 WKV 递推本体(delta-rule 状态更新——真正 RWKV 特有的数值部分,不含周边
Linear/LoRA 投影),直接用 MIL(coremltools 的 IR,不依赖 torch/fla)在真实 checkpoint 几何形状、fp16
下搭建,以 `coremltools.models.compute_plan.MLComputePlan`——CoreML 自己的逐算子设备分配计划——作为
证据(不是猜时间):

| 探测(限定计算单元 CPU_AND_NE,即只许 CPU 或 ANE 二选一) | 算子数 | 偏好=ANE | 偏好=CPU | 不限定(`ALL`)下是否仍是 CPU? |
|---|---:|---:|---:|---|
| WKV 单步,0.1B(H=12,D=64,fp16) | 30 | **0** | 30 | 是,全部 30 个 |
| WKV 单步,1.5B(H=32,D=64,fp16) | 30 | **0** | 30 | 是,全部 30 个 |
| WKV 链式 ×4 步,0.1B(fp16) | 105 | **0** | 105 | 是,全部 105 个 |

两种模型尺寸、两种链长共 168 个非常量算子,**没有一个**曾经被标记 `preferred=ANE`——即便 GPU+ANE
都不限定也一样,CoreML 自己的调度器把整个递推全部路由到 **CPU**。这不是探测方法本身坏掉或者这台机
器根本摸不到 ANE:一个正对照(fp16 批量 GEMM,1024×1024,读题阶段 Linear 投影的那类形状)确实拿到
`preferred=ANE`,**而且**在同一台机器上量到真实的 1.20 倍加速(Apple M5,ANE 可枚举、16 核)。第二
个对照——**解码形状的 GEMV**(`[1,2048]@[2048,2048]`,RWKV 自己的 r/k/v/o 投影在 bsz1 下真实的样
子,也就是任务假设"周边线性层随便哪个框架都能跑好"的那部分)——虽然被标 `preferred=ANE`,实测却比
纯 CPU **慢 18%**(单次、不摊批、不流水线的 ANE 调度,在这个尺寸下摊不平它自己的调度延迟)。

**怎么读**:根子和 §12.2(F0038)一样——bsz1 解码受带宽/发射次数限制,不是算力限制(硬上限约
123 GB/s,MLX-GPU 已跑到 79%);一个为批量矩阵乘设计的加速器,面对"每 token 把权重读一遍、做点小递
推数学"这种工作负载,结构上没什么可加的,而 RWKV-7 的顺序扫描(不做 chunkwise-parallel 重排,为了
保 oracle 位精确)恰恰是对它最不友好的形状。完整的逐层 CoreML 转换器(定长 decode/prefill 程序、显
式状态张量、oracle 门、tok/s)**没有去建**——建了也不会改变这个结构性结论,只会多花时间换来一个套
着 ANE 标签、实际跑在 CPU 上的递推。MLX-GPU(§12.1–§12.5)仍是完整、真正撑得住的 Apple Silicon 故事。

### 12.7 对标——Qwen3.5-2B 在 MLX 上,同协议、同机器(F0045)

Apple Silicon 这一档不是 RWKV 独占的([F0044](findings/0044-qwen35-mlx-feasibility.md)):Qwen3.5-2B
靠 `mlx-lm` 0.31.3 自带的原生实现就能跑在 MLX 上(其 Gated-DeltaNet 层有一个自写的真 Metal 核,不是
慢速兜底路径)。本节用与上面 §12.1/§12.3**完全相同的协议**测它——`mlx_port/bench_mlx_qwen35.py`
逐行照搬 `bench_mlx.py` 的 `bench_decode`/`bench_prefill`(16 步热身、128 步计时解码取 5 次中位、
1024 token 读题取 3 次中位)——同一台机器(Apple M5,MLX 0.31.2,`mlx-lm` 0.31.3)。

两边的"bf16"行确实都是 bfloat16——RWKV-7 自己在 §12.1 表头写的"fp16"是沿用本项目 CUDA 基线的命名
习惯,不是字面上的 float16(完整说明见 F0045)。int4 两边用的是同一套 MLX 原生 group-64 affine
仅权重量化方案(RWKV-7:`RWKV_MLX_QUANT=w4`;Qwen3.5:`mlx_lm.convert -q --q-bits 4
--q-group-size 64`)。

| 模型 | 精度 | 解码 tok/s(中位/最优) | 读题 tok/s(1024 token) | 峰值内存 |
|---|---|---:|---:|---:|
| RWKV-7 1.5B | bf16 | 32.8 / 33.6 | 1,691.6 | 3.38 GiB |
| Qwen3.5-2B | bf16 | 27.5 / 27.7 | **2,800.5** | 4.65 GiB |
| RWKV-7 1.5B | int4(w4g64) | 89.5 / 91.8 | 1,913.2 | **1.65 GiB** |
| Qwen3.5-2B | int4(g64) | 89.3 / 89.9 | **2,691.3** | 2.28 GiB |

同一次会话背靠背测得——两个模型同一轮跑、同样的负载条件。F0045 里还有一份可复现性检查(两边各自
独立重测一次,差幅 0.4–4.9%,落在本文档记录的 ±3–5% 抖动带内)以及规范引用(§12.3 的 RWKV-7 bf16
解码 37.3 tok/s,是更早一次会话测得的)作为交叉参照。

**split 结果,不藏着掖着**:RWKV-7 在 bf16 上赢单流解码(中位 **+19.3%**,32.8 对 27.5),int4 上是
统计意义上的平手(**+0.2%**,89.5 对 89.3——落在跑次间噪声范围内;换成引用 §12.3 的规范数字则差距
拉大到 +5.3%,所以 int4 解码应该算"太接近判不出",而不是 RWKV 干净赢)。Qwen3.5 在两档量化下都以显
著、抗噪声的优势赢读题(bf16 **+65.6%**、int4 **+40.7%**)——它交错的全注意力层(24 层里 6 层)能在
整个 prompt 窗口上吃到密集批量矩阵乘的并行度,而 RWKV-7 顺序的 WKV 递推即便分块也追不上。RWKV-7 在
两档都少约 27% 峰值内存,这在很大程度上是因为它标称就是个更小的模型(1.5B 对 2B),而非架构效率上
的胜利。完整方法学、可复现性检查、诚实的局限(Qwen3.5 没有正确性 oracle、这里也没跑压缩率尺)见
[F0045](findings/0045-qwen35-mlx-matched-benchmark.md)。

## 13. 与 Qwen3.5 对比(同引擎、同精度、尺寸匹配)

Qwen3.5(阿里巴巴)是**混合 Gated-DeltaNet** 架构——既有我们自己的实测确认(在自家 sglang
装置上 bring-up:24 层,18 层线性attention/GDN + 6 层全 attention,`interval=4`),也有
BlinkDL 自己参考实现的独立印证(`RWKV-LM/RWKV-v7/run_rwkv7_qwen35.py` 编码了完全相同的
`i%4!=3` 分层规则)。GDN 和 RWKV-7 自己的递推都是 DPLR(对角加低秩)族的成员——这是同代际
同族对比,不是"新老架构对撞"。Bo Peng 自己给出的 state 参数量公式把结构性差异说得很明白:
RWKV-7 的 state 是 `L·(2D + 64D)`,与上下文长度无关的常数;Qwen3.5 是
`L·¾·(3·6D + 2·128D) + L·¼·(2·2·256·T)`——第二项正比于 T,因为它 24 层里有 6 层是真
attention,需要随长度增长的真 KV cache。下面每一个并发天花板结果都能追溯到这一条结构事实。

**尺寸匹配**(最接近的可用 Qwen3.5 档位;尺寸并不相等,两边都全程标注):

| RWKV-7 | Qwen3.5 | 说明 |
|---|---|---|
| 1.5B | 2B | 主战场 |
| 7.2B | 9B | 主战场,int8/状态大小优势最明显的档位 |

权重:同引擎(sglang main,原生 `qwen3_5.py` 支持,不用改分支)、每行同精度。**Qwen3.5
必须用 `--dtype bfloat16`**——`float16` 会在一个混合 SSM 通用 Triton 核
(`causal_conv1d_triton.py`,Mamba2/Qwen3-Next/Qwen3.5 通用,不是我们该碰的东西)的
dtype 分支上崩溃,改用 checkpoint 原生的 bf16 即可绕开。这意味着本文档别处 RWKV-7 的
**fp16** 头条数字**不能**直接跟 Qwen3.5 的 bf16 数字比——下面每张表要么是 bf16 对 bf16,
要么明确说明是在引用 RWKV 的 fp16 优化数字以及原因(RWKV 的手写核只吃 fp16;bf16 走的是
RWKV 的原生/未优化路径)。

### 13.1 云档——RTX 5090

同精度(bf16,双方都是原生路径——RWKV 的 fp16 手写核不生效)单流:

| 档位 | RWKV-7(bf16) | Qwen3.5(bf16) | 胜者 |
|---|---:|---:|---|
| 1.5B / 2B | 256.6 | 335.8–336.3 | Qwen3.5 **+31.0%** |
| 7.2B / 9B | 87.7 | 96.0 | Qwen3.5 **+9.4%** |

RWKV-7 只有在自己的 **fp16** 手写核栈开启时才反超单流——这是不同精度下的比较,不是公平
的同精度读数:1.5B fp16 全核栈 397.3–409.8(对 Qwen3.5 +18–22%),7.2B fp16 全核栈
123.7(+29%)。**单流上 fp16 那档赢,靠的是核优化工作,不是 RWKV-7 架构在同精度下天生更
快——这两个说法要分清楚。**

同精度(bf16)峰值并发,完整扫描,`--cuda-graph-max-bs` 按各模型真实上限设置:

| 档位 | RWKV-7 峰值 | Qwen3.5 峰值 | 胜者 |
|---|---:|---:|---|
| 1.5B / 2B | **21,431.9 @ c=512** | 17,587.0 @ c=384 | RWKV-7 **+21.9%** |
| 7.2B / 9B | **6,171.3 @ c=256** | 4,295.8 @ c=128 | RWKV-7 **+43.7%** |

**这张卡上,RWKV-7 在两档都赢峰值并发——这正是本项目"重并发轻单流"方法论里判定为
决定性的那个指标。** 两张表并不矛盾:单流有利 Qwen3.5(bf16 下 RWKV 手写核不生效);
峰值并发有利 RWKV-7(Qwen3.5 那 6 层真 attention 需要随长度增长的 KV cache,把它的天花
板压得更低,呼应上面的 state 大小公式)。两个结果都如实报,谁也不藏。

顺带一条数据:Qwen3.5 原生也能跑 **FP8**(`--quantization fp8`,零额外代码)——这是
跟 RWKV int8 不同的量化档,不是匹配比较,但值得记一笔:Qwen3.5 FP8 在单流下比自己的
bf16 **慢 25–39%**(2B 是 206.6 对 336.0;9B 是 71.7 对 96.0)——跟 RWKV 自己 w8a8 在
小批量下的量化税是同一种模式。**真正的 int8 对 int8 目前做不到**:sglang 的 int8 路径
需要预量化 checkpoint、不支持边量化边跑,而 Qwen3.5 目前没有公开的预量化 int8
checkpoint——这是一个真实、如实披露的缺口,不是被跳过的测量(F0048)。

原始件:`bench/results/qwen35/*_5090*.json`;findings F0044–F0048。

### 13.2 桌面档——RTX 3090

同协议,更紧的 24GB 预算。峰值并发,bf16:

| 档位 | RWKV-7 峰值 | Qwen3.5 峰值 | 胜者 |
|---|---:|---:|---|
| 1.5B / 2B | **7,058.6 @ c=256** | 6,316.9 @ c=384 | RWKV-7 **+11.7%** |
| 7.2B / 9B | **1,796.3 @ c=128**(确认平台,c=192 复验 1,794.3) | 1,376.3–1,414.2 @ c=64(撞显存墙终止,仍在爬升) | RWKV-7 **≥+27.0%**(下限,非最终差距) |

7.2B/9B 那行报的是**下限**,不是最终差距:三次独立尝试把 Qwen3.5-9B 推过 c=64 都在这张
24GB 卡上真实 CUDA OOM(不是本项目常screens的那种 KV 饥饿假平台——每次尝试都用
`nvidia-smi` 核实过显存余量,并反推出 sglang 的显存池确切公式,证明在这张卡上没有任何
`--mem-fraction-static` 取值能同时供得起 Qwen3.5-9B 的 mamba-cache 预算、又扛得住
c≥64-68 时的真实请求突发——这是真实硬件天花板,不是调参没到位)。Qwen3.5-9B 的真实算力
瓶颈在这张卡上够不到;在更宽裕的 32GB 塔机上它到过 4,295.8。不要把"+27.0%"读成真实差距
——它是保守下限。

原始件:`bench/results/qwen35/*_3090*.json`(留在箱子上,不进本仓库);F0049。

### 13.3 Apple Silicon 档——MLX

Qwen3.5-2B 靠标准 `mlx-lm` 库(0.31.3)**开箱即用**跑在 MLX 上——它的 Gated-DeltaNet
层有真正的、非占位的 Metal 核,不是退化路径(F0044)。同协议、同机器的多轮匹配对比,
对手是 RWKV-7 1.5B 的 MLX 移植(F0045——完整表见 §12.7):**RWKV-7 赢 decode**
(bf16 +19.3%,int4 统计打平),**Qwen3.5 赢读题**(bf16 +65.6%、int4 +40.7%——它的
密集 attention 层能在整个 prompt 上吃到并行度,RWKV 顺序递推结构上追不上)。压缩率
(匹配 N=40/类,两边同测):RWKV-7 1.5B fp16 **0.5926** 对 Qwen3.5-2B bf16
**0.6719**——RWKV 领先,方向与下面云档的压缩率发现一致(F0045 附录)。**MATH500
avg@64 没有在这个平台跑**——尝试过的那次 session 在这台 Mac 上遇到真实内存压力后被
叫停,这是如实披露的缺口,不是静默漏测。Apple 神经引擎为什么不在这张图里,见 §12.6
(可行性门 FAIL,F0042)。

### 13.4 精度——压缩率与 MATH500 avg@64(云档读数)

| 模型 | 精度 | pooled bpb | N |
|---|---|---:|---:|
| RWKV-7 1.5B | fp16 | **0.6085** | 500/类(§2 规范值) |
| Qwen3.5-2B | bf16 | 0.6729 | 500/类,匹配协议 |

**RWKV-7 1.5B 压缩率优于 Qwen3.5-2B**,尽管自己是更小的模型——在这把尺上(本项目认定
的两把决定性尺之一,见 `feedback-benchmark-rigor`)RWKV-7 领先,且与 Apple Silicon 档
的同方向结果互相印证(两个平台、两套独立实现、同一个结论)。

**MATH500 avg@64。** 早先对 Qwen3.5-2B 跑这项测试时有一个真实的方法学 bug(把 RWKV 专用
的采样参数原样套在了非 RWKV 模型上,而且当时评测脚本压根没有 `presence_penalty` 接口),
导致 93.15% 的生成被截断,数字不可用;已定位根因、修好脚本、重新测过——完整过程见
[F0053](findings/0053-qwen35-math500-sampling-fix.md),包括 token 预算(16,384)是怎么
靠 pilot 试跑定下来的——这是一个披露在案、受成本约束的操作点,不是各模式的渐近上限。

| 模型 | 模式 | avg@64 | 截断率 | 平均生成 token | 说明 |
|---|---|---:|---:|---:|---|
| RWKV-7 1.5B | fake_think(1,500 token 预算) | 40.60%(原始)/ 40.42%(现 HEAD) | 14.2% | 581 | F0024,自家钦定尺 |
| Qwen3.5-2B | **非思考模式**(16,384 token 预算) | **67.63%** | **0.99%** | 4,659 | **本档头条数字**——与 Qwen3.5-2B 自己文档写明的默认运行模式一致 |
| Qwen3.5-2B | 思考模式(16,384 token 预算) | 47.72% | 52.4% | 11,880 | 与头条数字并列展示,不是 Qwen3.5-2B 在这个尺寸下的默认模式;是披露在案的下限,不是这个模式的上限(F0053) |

之所以把非思考模式定为 Qwen3.5-2B 的头条数字,是因为 Qwen3.5-2B 自己的模型卡片明确写着
"默认运行在非思考模式"——不是本项目挑了对哪边有利就用哪个模式,是模型自己文档写明的默认
用法决定的。如实读数:**Qwen3.5-2B 在这把尺上两个模式都领先 RWKV-7 1.5B**(67.63% 和
47.72%,对 40.4-40.6%)——按本项目"结论必须带数字"的一贯纪律,输了也一样如实报。思考
模式的截断率(52.4%)照实标注,不折进一个数字里藏起来:支撑这个数字的 pilot 试跑扫描
(F0053)在测过的每一档 token 预算(最高到 16,384)上都还在往上涨,而 32,768(Qwen3.5
自己文档写明的通用默认长度)纯粹因为耗时太长(单次测算要 16+ 小时)被排除——所以
47.72% 是一个真实、完整的 500×64 结果,但只是思考模式可达分数的下限,不是它的上限。

### 13.5 正确性

这次对比里 Qwen3.5 的输出不只是"看着连贯"——Qwen3.5-**2B** 已经用 BlinkDL 自己独立的
numpy fp32 参考实现做过 oracle 门(与本项目一直对自己 RWKV-7 核用的同一套严格标准),
在这次对比用到的两条服务路径上都验证过:mlx-lm(bf16)和 sglang(bf16)在探测 prompt 的
top-1/top-5 下一 token 分布上都与参考实现一致,差异符合正常 bf16 舍入(F0050)。
**Qwen3.5-9B 还没做这道门**——不假设它继承 2B 的结果,记为待办。

### 13.6 这次对比还没覆盖什么(如实说,不藏)

int8 对 int8(没有预量化 Qwen3.5 checkpoint,§13.1)。Apple Silicon 上的 MATH500(内存
受限,§13.3)。24GB 硬件上 Qwen3.5-9B 真实算力峰值(先撞显存墙,§13.2)。Qwen3.5-9B
正确性 oracle 门(§13.5)。移动端与嵌入式档位(本项目没有这类硬件)。以上都不是被静默
跳过——每一项都有对应的跟进记录。

---

*进行中(出数即更新本页):7.2B int4-GPTQ 的 MATH500 avg@64(§2/§4,决定 w4 Stage 2
要不要做);高带宽卡核融合工作持续推进(§7);3090 在 main 上的单请求阶梯;与重调参后
Albatross 的分尺寸对比格;Qwen3.5-9B 的 MATH500 avg@64(§13.4 目前只覆盖 2B 档,两种
模式都有,见 F0053)。*
