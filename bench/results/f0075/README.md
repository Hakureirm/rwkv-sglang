# F0075 — the HF port's B x T grid, re-measured after the source-read audit

`grid_after_source_audit.json` was produced by the benchmark committed inside
transformers-rwkv PR#2 itself — `benchmark_v2/benchmark_scripts/rwkv7_throughput.py`
(PR commit 746f44d) — with the flags and environment recorded in the JSON's
`environment` block. The harness lives with the PR rather than being duplicated here.
