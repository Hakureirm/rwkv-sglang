# sglang_overlay — RWKV-7 deliverable for sglang v0.5.10.post1

New + edited files mirroring the sglang package tree (`sglang/srt/...`). Deployed
into the box's wheel-installed sglang via `scripts/deploy.sh` (rsync, no build).
Edited copies of upstream files are pulled from the live install as the edit base.
